/**
 * Measure the visual brightness of a character when rendered in a monospace font.
 * Returns a value from 0 (empty/dark) to 1 (fully filled/bright).
 *
 * Stub: returns 0.5 for all characters. Real implementation will render
 * the character to a canvas and compute average pixel brightness.
 */
export function measureCharBrightness(
  _char: string,
  _fontFamily?: string,
  _fontSize?: number,
): number {
  return 0.5;
}
